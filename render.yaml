services:
  - type: web
    name: aetheric-v2
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: MISTRAL_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
